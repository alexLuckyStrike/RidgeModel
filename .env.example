# Backend (local Homebrew PostgreSQL on localhost:5432)
DATABASE_URL=postgresql://loadtrain_user:loadtrain_pass@localhost:5432/loadtrain
DB_HOST=localhost
DB_PORT=5432
DB_NAME=loadtrain
DB_USER=loadtrain_user
DB_PASSWORD=loadtrain_pass

# Docker variant (if you run postgres from docker-compose):
# DB_PORT=5433

# Nuxt proxy to backend
BACKEND_BASE=http://localhost:3001
